[package]
name = "data_source"
version = "0.1.0"
description = "Example module for ingesting external data via unfer_ffi"

[dependencies]
unfer_protocol = { path = "../../unfer_protocol" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
