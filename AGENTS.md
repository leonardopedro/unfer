# Agent Guidelines: Fock-Sirk Project

Welcome, Agent. This repository contains high-performance tools for quantum field theory (QFT) simulations using Nested Fock Spaces and Rational Krylov methods.

## Technical Architecture

### 1. Hybrid CPU/GPU Pipeline
The project implements a split-mode architecture for **"Inverse-Free" Rational Krylov** (SIRK):
- **CPU (Symbolic & Sparse)**: The forward sequence $w_k = (H - z_k I) w_{k-1}$ is generated on the CPU. It uses `nested_fock_algebra`'s symbolic CAS and sparse `FxHashMap` structures to handle the exponential branching of state trajectories.
- **GPU (Dense Tensor)**: Basis states are flattened into a `StateDictionary` and offloaded to the GPU. The Gram matrix $G_{j,k} = \langle w_j | w_k \rangle$ and reduced Hamiltonian $H_{proj}$ are computed using `candle-core` CUDA kernels for maximum throughput.

### 2. Field Theory & CAS Primitives
- **Hermitian Field Representations**: Fields are mapped to $a^\dagger + a$ and momenta to $i(a^\dagger - a)$.
- **Quadratic Ordering**: To satisfy mass gap requirements and ensure $\langle 0 | H | 0 \rangle = 0$, the CAS compiler (`cas.rs`) MUST drop pure scalar terms during the distribution phase.
- **BRST Symmetry**: Physics Hamiltonians (Navier-Stokes, Yang-Mills) must commute with the BRST charge $\Omega$. Always verify gauge invariance when adding new terms.
- **Combinatorial Explosion Avoidance**: High-order non-linear models (e.g., Yang-Mills with quartic terms) MUST bypass the symbolic `Expression::expand()` engine and build `Hamiltonian` `Operator` structures directly. Expanding $O(10^4)$ polynomial terms causes infinite recursion hangs in the `distribute` logic.

### 3. LaTeX-to-Fock Pipeline
- **Parser**: Uses `mathhook` to parse raw LaTeX math into a symbolic AST.
- **Mapping Logic**: The `latex.rs` module translates standard physics notation (like $\psi^\dagger \psi$) into internal operator strings (`c_... * a_...`). 
- **Validation**: When adding new LaTeX support, ensure that daggers ($\dagger, \dag$) correctly trigger the creation operator mapping.

### 4. Numerical Stability
- **Inverse-Free SIRK**: We avoid $O(N^3)$ linear solves $(H-z)^{-1}$ by utilizing the forward sequence. 
- **Gram Whitening**: The projection $H_{proj} = W^\dagger H_{raw} W$ uses Hermitian eigendecomposition (Stage 2 replaced the bare Cholesky that panicked on degenerate Gram matrices). If singularity occurs, reduce $m$ or adjust shifts $z_k$.
- **Unitary Time Evolution**: **Always** use `nalgebra`'s Padé approximant `exp()` for the reduced system to preserve unitarity and Hermiticity.

### 5. GPU Optimization & Environment
- **Device Selection**: Use `Device::cuda_if_available(0)`. 
- **Library Path Note**: On systems with multiple CUDA versions, ensure `LD_LIBRARY_PATH` points to the toolkit matching the driver (e.g., `/lib/x86_64-linux-gnu` for CUDA 12.2 coexistence).
- **CUBLAS Safety**: Initialization failures (`ARCH_MISMATCH`) often indicate a version conflict between `libcublas` and the active GPU.

## Maintenance Checklist

- [ ] **Quadratic Ordering Check**: Verify that `compile_expression` continues to strip zero-point energy constants.
- [ ] **LaTeX Mapping Check**: Ensure `compile_latex` correctly interprets $a_i^\dagger$ as a creation operator and $a_i$ as annihilation. Note that the `mathhook` LALRPOP parser strictly requires explicit multiplication symbols (`*` or `\cdot`) instead of implicit spacing.
- [ ] **Commutator Validation**: Ensure non-commuting operators are never reordered by the symbolic engine (avoid `.simplify()` where order matters).
- [ ] **PG/Random Start**: Adding a new `HamiltonianType` variant or changing `pauli_grover_a`/`random_start` defaults must be reflected in `QfmConfig`'s `..Default::default()` call sites (`prob_kernel/src/build.rs`, `qfm_text/src/model.rs`).
- [ ] **GPU Execution**: Run examples with `RUST_LOG=candle_core=debug` to confirm active CUDA kernel dispatch.
- [ ] **Vacuum Initialization**: Ensure `QuantumState::vacuum()` is properly initialized with at least one empty inner universe (`OuterBosonCreate(InnerBosonicState::vacuum())`) before applying inner operators.
- [ ] **Trust annotations (S21)**: `EffectKind::{Observe, Mutate}` — `observe`-kind effects auto-apply, `mutate`-kind (and un-annotated) always queue for human approval; `uk_registry_vetted` is console-only (UK-4501 to non-hook callers) and never touches the approval lane. Adding an `effect_kinds` entry must keep `GrantSet::is_subset_of` denying Mutate→Observe relabeling.
- [ ] **Admin console (S22)**: `unfer_edge/src/admin.rs` mints the admin exactly once from `UNFER_ADMIN_PRINCIPAL` (default `operator`); hard keys (`grants`, `auth`, `storage`, `backend`) are never patchable — any new hard-config key must be added to the refuse list, and the soft config must stay byte-identical on refusal.
- [ ] **Observability hygiene (S23)**: `uk_audit_append` and `uk_report_issue` MUST run `sanitize_sensitive` (api_key/token/secret/…) before storing; `uk_report_issue` stays a no-op unless `ERROR_REPORT_BINDING` is provisioned; dot-separated owner-log lines carry `component = "kernel.audit"` and the ring is capped (`OWNER_LOG_CAPACITY` 512, drop-oldest). Any new secret-plausible field must be added to the sanitizer, or the secret-scan gate test fails.
- [ ] **Release golden gate (S23/S24)**: `unfer_data::release` manifest CIDs map every deployable artifact byte→sha256; the golden test regenerates only via `UPDATE_GOLDEN=1` — a wrong byte in a module changes the manifest and fails CI.
- [ ] **Budgets / rate limits (S25)**: metered `uk_*` symbols are denied at the loopback chokepoint with `UK-4601 RATE_LIMITED` / `UK-4602 BUDGET_EXCEEDED` + an audit entry — never a post-hoc report; a `GrantSet`/code change must keep the windowed meter (UTC-day key) as the single denial point and `uk_meter_status` read-only.
- [ ] **Sensitive forward latch (S26)**: a `sensitive: true` observation sticks to the caller set; once set, `fetch`/`agent_spawn`/`blueprint_export`/`action_submit`/`gate_approve` are refused `UK-4701 SENSITIVE_LATCHED` until an operator clears it (S22 admin seam). Any new forward-mutating symbol must be added to the latch's refuse list.
- [ ] **Credential vault (S27)**: secrets go through `uk_secret_put/get/revoke` (opaque, grant-gated, encrypted at rest via the S15 `KeyRing`); they must never serialize into a `SessionBlob` snapshot or a `.cell` blueprint — `uk_snapshot`/`uk_blueprint_export` refuse to package a live secret.
- [ ] **Capability RPC (S28)**: capabilities are minted only at the loopback chokepoint carrying the caller's grant set; a returned capability stub is re-checked against the original caller and revoked ids are refused. Keep NDJSON/std.io as a degenerate single-capability mode and the C ABI stable.
- [ ] **Lean4 proof verification (S29)**: `prob_kernel::verify::verify_export` type-checks a `lean4export` NDJSON payload with `nanoda_lib` and reduces the proofs to a boolean (`ProofReport.verified`) — the proof-irrelevance analogue of `logos::deltanet`'s unique-normal-form hash. A rejected proof is `verified: false` by default; `LeanVerifySpec::strict` turns it into a hard `UK-4801`. Malformed/oversize payloads are `UK-4802` (`ProofExportInvalid`). Any new `uk_*` symbol must be added to `EXPECTED_SYMBOLS.txt`, the generated C header, `australVM`'s `UNFER_SYMBOLS`, and the `GrantSet.kernel` namespace; any new `KernelEvent` variant must be covered by the `handles.rs` event-type matchers.
- [ ] **Cadabra2 symbolic coupling (S30)**: `prob_kernel::symbolic::symbolic_analyze` couples the existing LaTeX engine with the external field-theory CAS Cadabra2 (GPL-3.0) as a **subprocess** (`cadabra2-cli`, `CADABRA_CLI` override), so the Rust binary never links GPL code. The expression (TeX-subset or the `c_0 * a_0` CAS dialect) is canonicalized; `normalize_to_cas_dialect` translates Cadabra2's braced output back into the dialect `compile_to_fock` accepts, closing the loop into a numerical `Hamiltonian`. `verified` is the zero-detection verdict (`H - H† = 0` → Hermiticity). `symbolic_derive` runs a multi-cell `.cdb` derivation pipeline and extracts named expressions. Engine-missing → `UK-4901`, malformed expression → `UK-4902`. Engine-dependent tests **skip** when `cadabra2-cli` is absent; `flake.nix` provides it in the dev shell. QG: the 3D gauge-fixed Hamiltonian derivation lives in `docs/qg_gauge_fixed_hamiltonian.cdb` (repaired from `yangqg3.cnb`/`qg6.cnb` — the notebooks don't run as-is: they're missing `\partial{#}::PartialDerivative`, a flat index set `{a,b,c}`, `\sigma`, and have an extra-brace typo plus an unbalanced spin-connection substitution; the repaired script runs cleanly and is exercised by `symbolic::tests::qg_*`). The repaired pipeline reaches `ex1` = the Einstein-Hilbert action density `eR` in the vielbein, then produces **book.tex's TEGR/teleparallel torsion scalar** `ℒ ≈ e(T_{ab}^b T^{ac}_c − ½T_{abc}T^{acb} − ¼T_{abc}T^{abc})` with `T_{abc} = X_{abc} − X_{bac}` (the `t0_tegr` output), **derives the polymomentum `pi_derived` by varying `ex1`** (the coefficient of `∂_α(d e^k_ρ)`; book.tex's `p^{ab} = π^α_{kρ} v_α e^ρ_c η^{cb}`), and finally **book.tex's 3D gauge-fixed Hamiltonian** `H_final` (book.tex line 8190: `ℋ = (1/16e)𝒮^{ab}𝒮_{ab} − (1/24e)𝒫² + ½𝒮^{ab}E_{ab} + ⅓𝒫E_a^a − e(𝒯^{ab}E_{ab} + 2𝒯^{ab}_b E_a + ½𝒯_{abc}𝒯^{acb} + ¼𝒯_{abc}𝒯^{abc} − 𝒯_{ba}^a𝒯^{bc}_c − ¼𝒯_{ab}𝒯^{ab})`), the Legendre transform of the TEGR Lagrangian in the `χ`/`v`-decomposed variables (coefficients derived via `𝒮 = 2eS`, `𝒫 = −4eT`). The `p^a·T_a` vector terms are dropped as a **boundary term**: the Hamiltonian acts on an initial wave-function with null probability at spatial infinity (physical Hilbert space with vanishing fall-off), and the Hamiltonian preserves that subspace, so `p^a·T_a = 2e·𝒯^{ac}_c·T_a` acts as zero on physical states. This is the intended teleparallel target: the TEGR identity says `eR = e·T_scalar + total divergence` (verified numerically), which is exactly what book.tex's `≈` (up to a divergence) means — it is not an error. **Yang-Mills (analogous, `docs/yang_mills_hamiltonian.cdb`):** the BRST Gauss constraint `G_y = c^j D_{jlμ}π^{μl} + c^j f_{jk}^l A_μ^k π^{μl} + ½i f_{jk}^l c^j c^k b_l` (notebook cell 1) plus the Legendre transform of the Weyl-gauge Lagrangian `L = ½π² − ½B²` (with `F_{0i}=π`, `¼F_ijF^{ij}=½B²`, the 3D epsilon identity verified numerically) gives `H_final = ½π² + ½B²`, book.tex's `H_W = −½π² − ½B²` in its `H = a† i∂₀a − L` convention. Exercised by `symbolic::tests::yang_mills_*`. **Densitized tetrad variables (ESA, `docs/qg_densitized_hamiltonian.cdb`):** the singular kinetic `(1/16e)S² − (1/24e)P²` (book.tex 8190) is transformed via `y = √e`, `\tilde e = √e e`, `S = y\tilde S`, `P = y\tilde P`; the `1/e` is absorbed into the field derivatives leaving the **flat** constant-coefficient hyperbolic operator `H_0 = (1/16)Δ_{\tilde S} − (1/24)∂²_y` (field-space d'Alembertian). By Strichartz (1973) a flat d'Alembertian on `L²(ℝ^N)` with smooth polynomial potentials is essentially self-adjoint (finite signal speed), so the transformed Hamiltonian is 100% ESA. Exercised by `symbolic::tests::qg_densitized_tetrad_absorb_1_over_e`. **Unitarity of the change of variables (`docs/qg_unitarity_check.cdb`):** three kernels are checked — (1) the Jacobian/measure `det(ỹe) = y³det(e) = y⁵`, i.e. `D\tilde e = J De`; (2) the half-density (van Vleck) norm kernel `y⁵·y⁻⁵ = 1` making the point transformation unitary; (3) the flat-operator Hermiticity (Lagrange) identity `ψ∂_{xx}φ − φ∂_{xx}ψ = ∂_x(ψ∂_xφ − φ∂_xψ)` — a total-derivative boundary term vanishing on the physical Hilbert space. All three must vanish; exercised by `symbolic::tests::qg_densitized_change_of_variables_is_unitary`. **Starobinsky R+αR² (`docs/qg_starobinsky_hamiltonian.cdb`):** the action `f(R) = (M²/2)R + αR²` (α > 0) is reduced to the 3D gauge-fixed Hamiltonian. `scalaron_check` verifies the ghost-free scalar-tensor equivalence `f(R) = (M²/2)ψR − U(ψ)` at `ψ = 1 + 4αR/M²`, `U = (M⁴/16α)(ψ−1)²` (→ 0; no Ostrogradsky ghost). `V3_check`/`dV3_check`/`V3_min` prove the 3D spatial potential `V3(R_c) = −(M²/2)R_c + αR_c²` is a parabola in the 3-curvature (the conformal mode) with global minimum at `R_c = M²/(4α)`, value `−M⁴/(16α) > −∞` — the conformal-factor regularization (→ 0). `Vphi_zero`/`Vplat`/`dVphi_zero` pin the Einstein-frame Starobinsky potential `V(φ) = (M⁴/16α)(1−e^{−√(2/3)φ/M})²`: V(0)=0, plateau M⁴/(16α) at φ→+∞, dV/dφ=0 at φ=0 (→ 0; V ≥ 0 as a square — bounded below). The gauge fixing of the **spatial field-derivative variables** follows the Navier-Stokes pattern (book.tex §4159-4197): as NS promotes the spatial gradients `u_{i,j} = ∂_j u_i` to independent canonical fields and fixes them to the values of the field derivatives, so here the scalaron gradient product `(∂_iφ)(∂^iφ)` (`Dphi2 → grad2`) and the conformal-mode curvature `R_c = Ω⁻⁴R̄_c − 8Ω⁻⁵∇̄²Ω` (with the derivative variable `∇̄²Ω` solved and substituted back) are independent phase-space variables fixed to the values of the actual spatial derivatives (each `gf_check_*` → 0), so the Legendre transform `H_gf = ½π² + ½(∂φ)² + V(φ) − (M²/2)R_c + αR_c²` carries genuine products of **spatial** field derivatives (`grad2`, `R_c²`) — exactly as the NS Hamiltonian carries `u_j·u_{i,j}` (the time-derivative variables are handled by the standard Legendre transform, not gauge-fixed) — alongside `H_final = ½π² + ½(∇φ)² + V(φ)`. `constraint_check` resolves the Hamiltonian constraint solved for the conformal-mode curvature (→ 0). Exercised by `symbolic::tests::qg_starobinsky_*` and `prob_kernel/tests/qg_starobinsky_sirk.rs` (Cadabra2→SIRK full pipeline; the Fock realization is the quantized scalaron `Σ m·N_i`, `m² = M²/(12α)` the published scalaron mass). **Part 5 — the 3D gauge-fixed Hamiltonian FROM the classical action** (the Einstein-module pipeline of `qg_gauge_fixed_hamiltonian.cdb`: action → vary → polymomentum → Legendre transform → H): `action_J`/`action_R2_check` put the R² action in its ghost-free scalar-tensor form `(M²/2)ψR − U(ψ)`, `ψ = 1 + 4αR/M²` — verified identical to `(M²/2)R + αR²` (→ 0); `action_gf` is the 3D gauge-fixed action density (synchronous gauge, conformal-mode variables: the scalaron kinetic `½(∂₀φ)²`, the spatial-derivative variable `½Dphi2` fixed to `grad2` in the Hamiltonian, the Starobinsky potential, and the conformal-mode parabola `(M²/2)R_c − αR_c²` — the R² contribution to the spatial potential); `pi_derived` is the **polymomentum BY VARIATION** (the exact `vary(action, φ -> dφ)` pattern, `∂₀(dφ) -> π`, `dφ -> 0`, `∂_i(dφ) -> 0`): the varied kinetic is `(∂₀φ)·π`, so the canonical momentum is `π = ∂L/∂(∂₀φ) = ∂₀φ` (`pi_check` → 0); `H_action` is the Legendre transform `H = π·∂₀φ − L` with the velocity solved for the momentum and the spatial derivative variable fixed to the derivative values — `H = ½π² + ½(∂φ)² + V(φ) − (M²/2)R_c + αR_c²`, identical to Part 4's `H_gf` (`leg_check` → 0); `Rtil_3p1` closes the gravitational sector via the 3+1 (ADM, synchronous-gauge) split `R = R3 + (K² − K_ijK^ij)`, whose spatial potential is the α-stabilized parabola. **Part 5.6/5.7 — the gravitational sector FROM the action** (the metric analogue of `pi_derived`): `action_grav = (M²/8)((∂₀q)² − dqsq) + (M²/2)R_c − αR_c²` is the gravitational part of the 3+1 action in the metric time-derivative variables (`K = ½∂₀q`, `K_ijK^ij = ¼dqsq`; `dqsq` is the promoted derivative-variable product — no digits, since Cadabra2's index parser treats digit-suffixed names as indices); `pi_grav` is the **metric polymomentum BY VARIATION** (the same `vary` pattern, `∂₀(dq) -> Π`): `Π = ∂L/∂(∂₀q) = (M²/4)∂₀q` (`pi_grav_check` → 0); `H_grav` is its Legendre transform `(2/M²)Π² + (M²/8)dqsq − (M²/2)R_c + αR_c²` (`leg_grav_check` → 0, with the `M²(M⁴)⁻¹ → M⁻²` power-collapse rules); `H_total` is the **full 3D gauge-fixed Hamiltonian** derived from the complete classical action — `½π² + ½(∂φ)² + V(φ) + (2/M²)Π² + (M²/8)dqsq − (M²/2)R_c + αR_c²` (the reduced physical form `H_final` follows on the Hamiltonian constraint).
- [ ] **Logos CNL→UNF coupling (S31)**: `prob_kernel::logos::logos_compile` parses a CNL sentence with an embedded L0 lexicon, compiles to CoreIR, reduces to an interaction-net unique normal form (UNF), and read-backs the result + content-addressed `unf_hash`. Exposed as `uk_logos_compile` over the C ABI; unparseable input → `UK-4803` (`LOGOS_COMPILE_FAILED`). The report's `verified` is a confluence self-check (re-reducing the same sentence yields an identical UNF). Formal confluence: `logos/lean/Confluence.lean` machine-verifies the diamond property, Church–Rosser confluence, and uniqueness of normal forms via kernel-computed `rfl` (`Eq.refl`) over the enumerated finite state space. The proof is exported to the `lean4export` NDJSON format 3.1.0 (official `leanprover/lean4export`, not the legacy `ammkrn` tool which emits the old textual format 2.0.0 nanoda rejects) and pinned at `prob_kernel/tests/fixtures/confluence.ndjson`, where `verify_export` re-verifies it in nanoda — the proof term must stay `rfl`/kernel-computed, since `native_decide`/`decide` emit `Lean.ofReduceBool` + `_nativeDecide_*` terms that nanoda (an independent checker) cannot reduce. Regeneration needs the official `lean4export` on the matching toolchain (provisioning note in `docs/LOGOS.md`). New symbols follow the S29 registration checklist (EXPECTED_SYMBOLS.txt, generated C header, `UNFER_SYMBOLS`, `GrantSet.kernel`).
- [ ] **QED perturbative validation (S32)**: `fock_sirk/tests/qed_validation.rs` checks the Fock/SIRK machinery against published QED results. Free photon: vacuum E=0 (normal ordered), one-photon Ritz = `ω = |k|` (massless dispersion), two-photon additivity `2ω`, and the Casimir-cavity spectrum `ω_n = nπ/d` (whose regularised zero-point sum is `E/A = −π²ħc/720d³`) — all exact. One-photon exchange: the framework's matrix elements `⟨1_i|H|0⟩` reproduce the exchange vertex and assemble to `δE(r₁)−δE(r₂) = −e²/4π(1/r₁−1/r₂)` (Coulomb's law, Zee QFTN §I.3) to <2% with the radial-shell mode set, and a small SIRK solve reproduces the exact displaced-oscillator shift. Pair production: SIRK diagonalizes the γ↔e⁺e⁻ sector **exactly** (non-perturbative — it is not a diagrammatic expansion); at weak coupling the exact eigenvalue reduces to the published one-loop self-energy `δE = Σ_p c_p²/(ω−E_p−E′_p)` (vacuum polarization) to ~1e-3, scales as `e²` (O(α)), and DEPARTS from perturbation theory as the coupling grows (exact vs one-loop differ >10% at e=1) — the test's demonstration that the machinery goes beyond PT. Kinematics: min pair energy = `2√(m²+(q/2)²)` (an on-shell massless photon cannot pair-produce). U(1): `[H, Q] = 0` for `Q = Σe†e − Σp†p`. Builders live in `nested_fock_algebra::models` (`qed_free_photon`, `qed_cavity_frequencies`, `qed_coulomb_radial_modes`, `qed_static_charge_interaction`, `qed_charge_operator`, `qed_pair_production`). Two framework-convention gotchas the builders encode: the γ↔e⁺e⁻ vertex MUST list the **positron annihilation before the electron** (`(e†p†a)† = a†p_{n+j}e_j` under the framework's canonical fermion ordering — the reverse order gives `⟨pair|H|γ⟩ = −⟨γ|H|pair⟩`, i.e. H ≠ H†); and a multi-occupation state must be built as **one universe with the correct inner occupation** (e.g. `{i:2}`), not as two separate outer universes — the inner ladder operators are the framework-native construction and give `⟨0|H|0⟩ = 0` automatically and correct additivity `n|n⟩ = n|n⟩` at any occupation (an earlier "inner ops leak at occupation ≥ 2" note was a misunderstanding: the leak only appeared when a two-photon state was (mis)built as two outer universes and then measured with inner operators). The pair-production sector solve is well-behaved only at Krylov m=6 (larger m lets the basis wander into higher pair/multi-photon states and the isolated dressed-photon Ritz value is lost). The Coulomb comparison is scalar/timelike-photon (the vector gauge structure flips the like/opposite-charge sign but leaves the `e²/4πr` magnitude). Dynamics (SIRK restarted Krylov): `qed_unitary_evolution_energy_conservation` evolves a photon superposition and verifies exact norm (unitarity) + energy (`⟨ψ|H|ψ⟩`) conservation.
- [ ] **QCD perturbative validation (S33)**: `fock_sirk/tests/qcd_validation.rs` checks the Fock/SIRK machinery against published perturbative QCD. Color factors: `C_F = (N_c²−1)/(2N_c) = 4/3`, `C_A = N_c = 3`, `T_R = 1/2` computed from the SU(3) structure constants (not hard-coded). One-gluon exchange: the framework's matrix elements `⟨1_i|H|0⟩` carry the `C_F = 4/3` color factor and assemble to the Coulomb part of the quark–antiquark potential `δE(r₁)−δE(r₂) = −C_F α_s(1/r₁−1/r₂)` (the Cornell-potential coefficient, P&S §16.2) to <1% with the radial-shell mode set; a small SIRK solve reproduces the exact displaced-oscillator shift. β-function: `β₀ = (11/3)N_c − (2/3)N_f` (Gross–Wilczek–Politzer 1973) gives the published 11 (pure glue), 9 (N_f=3), 7 (N_f=6), the `(33−2N_f)/3` numerator, and asymptotic freedom (`β₀ > 0` ⇔ α_s decreasing) flipping sign for N_f > 33/2. **Numerical results (published + experimental)**: two-loop `β₁ = (34/3)N_c² − (10/3)N_cN_f − 2C_FN_f` = 102/64/26 (Jones, Caswell 1974); the two-loop running coupling (fixed-step RK4, deterministic) turns the PDG world average `α_s(M_Z) = 0.1179` into `α_s(M_τ) = 0.339` (published `0.314 ± 0.030`; the one-loop formula gives only ~0.27, so the test discriminates the perturbative order) with a round-trip self-consistency check; and the parton-model `R = N_c ΣQ_f² = 2` (u,d,s), `10/3` (u,d,s,c), `11/3` (u,d,s,c,b), `5` (six flavours), confirmed by PDG to ~10%. Builders in `nested_fock_algebra::models` (`qcd_su3_f`, `qcd_color_factors`, `qcd_one_gluon_exchange`, `qcd_beta_function`, `qcd_beta_two_loop`, `qcd_running_coupling`, `qcd_alpha_s_running`, `qcd_r_ratio`, `QCD_ALPHA_S_MZ`). The gluon-exchange builder reuses the QED outer-number-operator convention; the color factor enters as `C_F·e²` inside the amplitude square (so δE carries C_F), NOT as `√C_F` on the linear coefficient. The α_s running uses a crude single-flavour-threshold approximation, so the α_s(M_τ) window is generous (0.05); the two-loop β₁, R-ratio and the exact running-coefficient checks are exact. SIRK-based (Hashimoto inverse-free rational-Krylov): `qcd_gluon_dispersion_sirk` diagonalizes the free gluon field `H = Σ |k| N_k` in Fock space, reproducing the massless dispersion `ω = |k|` (perturbative QCD); `qcd_mass_gap_sirk` contrasts the massless free gluon (E→0 as k→0) with the confined `yang_mills_lattice`, whose SIRK cross-sector solve gives a positive mass gap `≈ g²/2` (the lattice origin of the QCD mass gap / Millennium-Prize confinement statement). **Cadabra2-derived Hamiltonian**: `qcd_ym_hamiltonian` implements the `.cdb`-derived `H_final = ½π² + ½B²` (the Weyl-gauge Legendre transform) with the magnetic field `B` a **genuine function of `A`** (`B = (A₀−A₁) + ½g·A₀A₁`, the lattice-difference derivative plus a non-abelian `f`-term → a quartic `B²`), built through the framework's **CAS compiler** (`compile_to_fock`) — the framework-native **inner** construction, which normal-orders and strips the `[a,a†]=1` zero-point so `⟨0|H|0⟩ = 0` is guaranteed (the nested-Fock vacuum rule; the earlier claim that inner ops "leak at occupation ≥ 2" was a misunderstanding — it only appeared when a multi-occupation state was (mis)built as separate outer universes, and `compile_to_fock`'s quadratic ordering was in fact a no-op, now fixed to genuinely normal-order). The SIRK/GPU reduction engine (`solve_forward_sirk` → `whiten_gram` Hermitian eigendecomposition → `h_proj`, CUDA-capable via `best_device()`) reduces it to a Hermitian, bounded-below spectrum with positive excitation gaps (the Millennium-Prize positivity). The brute-force full SU(3) `yang_mills_hamiltonian` (76K terms, indefinite `−½π²` in book.tex's `H_W` convention) is NOT SIRK-tractable — the reduced B(A) form is. Dynamics (SIRK restarted Krylov): `qcd_unitary_evolution_energy_conservation` evolves a gluon superposition and verifies exact norm (unitarity) + energy conservation. **Non-perturbative gluon self-energy**: `qcd_gluon_self_energy_nonperturbative` SIRK-diagonalizes the gluon ↔ q q̄ sector EXACTLY (not perturbative — no loops); the weak-coupling limit of this non-perturbative result reduces to the *perturbative, non-SIRK* quark-loop self-energy `T_R·Σc²/(ω−E−E′)` (color ratio → `T_R = 1/2`, published), and at strong coupling the exact result departs from perturbation theory (SIRK is non-perturbative). Builder: `qcd_pair_production` (gluon↔qq̄ with the `√T_R` color vertex). Complements the non-perturbative Yang-Mills lattice mass-gap and the symbolic BRST-Gauss tests already in the repo.
- [ ] **QG numerical validation (S34)**: `fock_sirk/tests/qg_validation.rs` checks the framework against published *numerical* gravity results — the semiclassical/Newtonian limit of the TEGR/teleparallel Hamiltonian derived symbolically in `prob_kernel::symbolic`. Planck scale from CODATA G/ħ/c: ℓ_P = 1.616×10⁻³⁵ m, t_P = 5.391×10⁻⁴⁴ s, m_P = 2.176×10⁻⁸ kg, E_P = 1.221×10¹⁹ GeV (exact), with the dimensional identities ℓ_P·m_P = ħ/c and ℓ_P = c·t_P. Gravitational redshift (Pound–Rebka): z = g·Δh/c² = 2.46×10⁻¹⁵ (published ~2.5×10⁻¹⁵, measured to ~1%). Mercury perihelion advance Δφ = 6πGM/(c²a(1−e²)) = 42.97″/century (published 43.0). Starlight deflection at the Sun's limb δ = 4GM/(c²b) = 1.750″ (published 1.75″, Eddington). GPS gravitational time dilation GM/c²(1/R−1/(R+h)) = 5.29×10⁻¹⁰, ~45.7 µs/day (published ~45.9). TEGR ↔ GR equivalence (the project's central QG claim): for flat FLRW, R = 6(Ḣ+H²), T = −6H², and both give the same Friedmann equation 3H² = 8πGρ (TEGR is classically equivalent to GR, `eR = e·T + divergence`; numerically R+T equals the divergence term). Newtonian limit: Φ = −GM/r = −6.26×10⁷ m²/s² at Earth's surface, r_s(Earth) = 8.87 mm. Builders in `nested_fock_algebra::models` (`qg_planck_units`, `qg_gravitational_redshift`, `qg_perihelion_precession`, `qg_light_bending`, `qg_gps_rate`, `qg_flrw_scalars`, `qg_newton_potential`, `QG_G`, `QG_HBAR`, `QG_C`). These are classical-GR/CODATA numerical anchors — the published predictions of the theory being quantized (the test-file naming `qg_light_bending`/`qg_gravitational_redshift` collides with the model fn names, so the test fns are suffixed `_eddington`/`_pound_rebka`). SIRK-based: `qg_graviton_dispersion_sirk` builds the free graviton field `H = Σ c|k| N_k` in Fock space and diagonalizes it with the Hashimoto inverse-free rational-Krylov solver, reproducing the massless dispersion `ω = c|k|` (GWs travel at c — the published GW170817/GRB170817A constraint `|Δv/c| < 1e-15`). **Cadabra2-derived Hamiltonian (outer nested Fock space)**: `qg_tegr_hamiltonian` implements the kinetic part of the `.cdb`-derived TEGR `H_final` (book.tex 8190, `(1/16e)𝒮²`) built from **outer** ladder operators and normally ordered so `⟨0|H|0⟩ = 0`; the SIRK spectrum is Hermitian (self-adjoint in the truncation) and bounded below with positive gaps — the essentially-self-adjoint (ESA) property the project derives via Strichartz for the densitized d'Alembertian. The brute-force full TEGR `H_final` (in abstract S/E/T torsion variables) and the full SU(3) `yang_mills_hamiltonian` are not directly SIRK-tractable; the outer-Fock reduced kinetic forms are. Dynamics (SIRK restarted Krylov): `qg_unitary_evolution_energy_conservation` evolves a graviton superposition and verifies exact norm (unitarity) + energy conservation; `qg_gravitational_wave_phase_sirk` evolves a single graviton mode and verifies its phase advances as `φ = ωt = c|k|·t` — the GW oscillates at the massless frequency (speed c, the LIGO/Virgo observed frequency evolution). **Starobinsky scalaron (Cadabra2-derived, `docs/qg_starobinsky_hamiltonian.cdb`)**: `qg_starobinsky_scalaron_sirk` implements the gauge-fixed scalar sector `H_final = ½π² + ½(∇φ)² + V(φ)` quantized as the massive scalaron `H = Σ m·N_i` (inner ladder operators; `m² = M²/(12α)` is the curvature of the quadratic Starobinsky potential at the vacuum) and verifies: the normal-ordered vacuum energy is 0, the one-scalaron energy is exactly `m` per mode, the two-scalaron energy is additive `2m` (inner construction, correct at any occupation), doubling the scalaron mass doubles the spacing (the published scalaron-mass content of the Starobinsky potential), and the SIRK-reduced spectrum is Hermitian, bounded below with positive excitation gaps — the boundedness claim of the αR² stabilization (no conformal-mode −∞, the Millennium-Prize-style positivity of the quantized theory). **Starobinsky down-to-earth numerical validation (`fock_sirk/tests/qg_starobinsky_validation.rs`)**: the classical content of the quantized R² Hamiltonian is checked against mainstream-GR/Newtonian anchors (no cosmology — the tests are the solar-system and laboratory numbers a correct theory of gravity must reproduce). `qg_starobinsky_newtonian_limit_yukawa` checks the linearized weak-field potential `Φ(r) = −GM/r (1 + ⅓e^{−mr})` — the published f(R) Yukawa form (coefficient ⅓ from the trace equation): Newtonian at r ≫ 1/m (deviation exactly ⅓e⁻¹⁰ at r = 10/m) and the 4/3 short-range force enhancement. `qg_starobinsky_solar_system_gr` uses the Planck-heavy scalaron `m = 1/√(12α)` (Compton wavelength ≈ ℓ_P/√12 ≈ 5e-36 m, so `e^{−mr} < 1e-30` at any macroscopic distance — R² gravity passes solar-system tests) to reproduce the published GR anchors exactly: Mercury perihelion 43.0″/century, GPS rate 5.29e-10 (~45.9 µs/day), Pound–Rebka 2.46e-15, Sun-limb deflection 1.75″, Earth-surface Φ = −6.26e7 m²/s². `qg_starobinsky_scalaron_massive_dispersion_sirk` — the **numerical Hashimoto algorithm** — diagonalizes the quantized scalaron field `H = Σ √(k²+m²) N_k` (nested Fock space, inner ladder operators) and the Ritz values reproduce the massive Klein–Gordon dispersion `ω = √(k²+m²)` (the R² theory's scalar mode is massive, unlike the massless graviton `ω = c|k|`): vacuum 0, one-scalaron = ω per mode, group velocity `dω/dk = k/√(k²+m²) < 1` rising monotonically toward c (massive propagation is subluminal), and the m→0 limit recovers the massless graviton sector (consistent with `qg_graviton_dispersion_sirk`). `qg_starobinsky_derivative_variable_brst` — the **gauge constraints for the variables representing the spatial field derivatives**, the Navier-Stokes pattern (book.tex §4159-4197): the scalaron's spatial gradients `g_i = ∂_iφ` are promoted to independent canonical fields (inner Hermite-basis modes, **no lattice** — the Hamiltonian is defined in the nested-Fock Hermite basis) and the BRST charge `Ω = Σ_i g_i·c_i` fixes each derivative variable to the value of the field derivative (the gravity analogue of NS's `Ω = Σ_j u_{j,j}·c_j`; the `gf_check_Dphi2 → 0` fixing of the .cdb). Assertions: `Ω² = 0` (nilpotent, first-class), the promoted gradient fields are constants of the motion `[H, g_i] = 0` (no momenta on the derivative modes — the Eulerian block structure; products of the derivative variables `½Σg_i²` survive in `H = m·N_0 + ½Σg_i²`, exactly as the NS Hamiltonian carries `u_j·u_{i,j}`), `[H, Ω] = 0` (BRST-closed), and the bare truncated SIRK flow from a genuinely unphysical ghost-carrying state grows the Ω-content (`‖Ωψ₀‖ = √3 → ‖Ωψ(t)‖ > √3` under aggressive Krylov truncation) — the physical subspace is not invariant under the truncated dynamics, which is why the BRST projector rides along in the solve. Builders in `nested_fock_algebra::models`: `qg_starobinsky_scalaron_mass` (`m = 1/√(12α)`), `qg_starobinsky_weak_field_potential` (the Yukawa form), `qg_starobinsky_scalaron_field` (`Σ√(k²+m²)N_k`), `qg_starobinsky_derivative_brst` (`Ω = Σg_i·c_i`), `qg_starobinsky_gauge_fixed_scalaron` (`H = m·N_0 + ½Σg_i²`).
- [ ] **NS numerical validation (S35)**: `fock_sirk/tests/ns_numerical_validation.rs` checks the Eulerian Navier–Stokes formalization (the `ThreeComponent` affine-fiber generator `ns_eulerian_fiber`) against published *experimental* fluid-dynamics results and estimates. Kolmogorov turbulence: `η = (ν³/ε)^{1/4}`, the identity `Re_η = 1`, the −5/3 spectrum with the experimental constant `C_K ≈ 1.5` (Sreenivasan & Antonia range 1.4–1.7, exponent ratio `E(8)/E(2) = 2^{−10/3}`), the exact 4/5 law `⟨δu_L³⟩ = −(4/5)εr`, and the Taylor dissipation relation `ε = 15ν⟨(∂u₁/∂x₁)²⟩`. Hagen–Poiseuille pipe flow `Q = πR⁴Δp/(8μL)` (verified to great precision) and the Darcy–Weisbach laminar friction `f = 64/Re` (the Moody-chart line). Stokes' law `F = 6πμrU` (Millikan oil-drop base), the Oseen correction `1 + 3Re/16`, and the `r³`-scaling of terminal-velocity balance. Blasius flat-plate boundary layer: the exact constants `δ* = 1.7208`, `θ = 0.6641`, `H = δ*/θ = 2.5916`, `C_f = 0.664/√Re_x` (matches experiments to ~3%). Transition Reynolds numbers (pipe `Re_c ≈ 2300`, plane Poiseuille `5772.22` Orszag 1971, flat plate `≈ 5×10⁵`) and the vortex-shedding Strouhal number `St ≈ 0.2` (Roshko 1954). Lamb–Oseen vortex core growth `r_c(t) = √(4νt)` (√t scaling) and peak vorticity `ω_max = Γ/(4πνt)` (the invariant `ω_max·t = Γ/4πν`). **SIRK-based machinery test**: `ns_sirk_laminar_decay_rate` verifies the Ehrenfest identity `i⟨[H,u]⟩ = 4κ⟨u⟩ + 4c` (exact on occupation/coherent probes) and, with `κ = −νk²/4`, reproduces the Newtonian free decay of a laminar Fourier mode `du/dt = −νk²u` — the SIRK-restarted evolution measures the decay rate `νk²` to <1e-2. Two framework conventions the tests encode: `u = a†+a`, `π = i(a†−a)`, `H = {π,V}` (the symmetrization carries the factor 2, giving `i⟨[H,u]⟩ = 4κ⟨u⟩+4c`), and the `|0>+|1>` coherent-like probe is built as one outer universe per occupation (`modes:{0:0}` must drop the mode, else `{0:0} ≠ {}` splits the ket).
- [ ] **Event-sourced Session (H3)**: `prob_kernel::Session` is a write-ahead event log: each mutating op (`Create`/`SetPrior`/`SetHamiltonian`/`Evolve`/`Condition`/`CompactStart`/`CompactEnd`) appends a typed `SessionEvent { seq, op, spec, source, ts }` **before** it applies (rolling back the record on error), and `SessionBlob` is now `{ format_version, events, … }` (folded legacy fields kept). `restore` replays with strict-monotonic-seq + bracket-balance validation; a version mismatch → UK-1006 `SESSION_LOG_VERSION`, an orphaned `CompactStart` lock (crash between start/end) → UK-1007 `SESSION_COMPACTION_ORPHANED`, a busy session → UK-1008 `SESSION_COMPACTION_BUSY`, a bad/inside-lock fork boundary → UK-1009 `SESSION_FORK_RANGE`. Rules: compaction boundaries must be settled records (never `Evolve` — tool-pairing, and never `CompactStart`), the boundary must be in range, QFM sessions cannot be compacted (the pipeline is not serializable), and mutating ops are refused while the lock is open. `derived_log()` replaces the summarized prefix with the `CompactEnd` summary node; `fork_at(seq)` replays the prefix and diverges. The debug-build invariant `fold(events) ≡ live` (via `debug_assert_reconstructable`) replays the full raw log after every op — keep replay deterministic (SIRK evolve + QFM generate are deterministic; the `qfm` flag and query are recorded in the `Evolve` event so replay mirrors the live dispatch exactly). New symbols `uk_session_fork`/`uk_session_compact` follow the S29 registration checklist (EXPECTED_SYMBOLS.txt, generated C header, `UNFER_SYMBOLS` + `ecma.rs` dispatch arms, `GrantSet.kernel`); they are session-local and must NOT be added to `SENSITIVE_BLOCKED_SYMBOLS`. Any new UK-1xxx session code or new `SessionEventSpec`/`SessionOp` variant must keep the version marker bump + restore validation + PROTOCOL.md row in sync.
- [ ] **Certificate ledger (Plan R)**: the UTXO/carbon-certificate state machine lives in
  `unfer_consensus::certs` (`CertificateLedger` + `SparseMerkle`). `CertificateOp`s
  (Mint/Transfer/Burn) are a `ConsensusTransaction` variant applied by `ConsensusNode::sync`.
  Minting is disabled unless a `MintAuthority` is configured. Any new op must preserve the
  invariants: conservation on transfer (UK-7002), no double-spend (UK-7004), owner-only spend
  (UK-7005), mint-authority check (UK-7001). Keep the ledger deterministic (same log → same
  root). See `docs/PLAN_REFI_EXCHANGE.md`.
- [ ] **Unified auction (Prebid-model)**: the deterministic auction engine lives in
  `unfer_consensus::auction` (`AuctionLedger`); settlement lives in `unfer_taler::auction`
  (`AuctionService`, which needs both `TalerExchange` and `CertificateLedger`). `AuctionOp`s
  (Open/Bid/Close) are a `ConsensusTransaction` variant applied by `ConsensusNode::sync`. The
  clearing rule is a pure function of the recorded bids — highest `price_per_unit` wins, ties
  break to the earliest `seq` — so every node replays the same log and converges on the same
  winner. Two markets, one mechanism: carbon credits (seller escrows the credit certificate,
  winner gets credits, loser refunded) and publicity inventory (AdSense alternative: payment
  only, no ledger asset). Payments are escrowed e-coins whose face value must equal the bid
  total (denomination model, mirroring Taler); the operator derives each escrow DID/key from
  `(operator pubkey, lot_id, party, coin)` so only it can settle. Any new auction rule must
  keep the engine deterministic (no wall-clock/random in `AuctionLedger`) and the escrow
  conserving (same `total_supply` before/after). New `uk_auction_*` symbols follow the S29
  registration checklist (EXPECTED_SYMBOLS.txt, generated C header via
  `python3 unfer_ffi/gen_unfer_kernel_h.py`, australVM `UNFER_SYMBOLS` + `ecma.rs` dispatch
  arms + `SENSITIVE_BLOCKED_SYMBOLS`). `uk_auction_close` both mutates and writes, so its
  marshaling uses a single fixed-buffer call, not the probe-then-copy `buf_out` protocol.

## Crate Layout (Stages 1–28 complete)

- `nested_fock_algebra` — symbolic CAS + Fock-space algebra (improved: `adjoint()`, `prune`, `truncate_top_k`, bounded expansion).
- `fock_sirk` — SIRK solver (improved: GPU-optional, Gram whitening, BRST projection, restarted Krylov, state reconstruction).
- `unfer_protocol` — serde types, UK-#### codes, repair hints (the shared contract).
- `prob_kernel` — Born-rule layer: `Session` with `evolve`/`probability`/`condition`/`snapshot`.
- `unfer_ffi` — handle-based C ABI: 21 `uk_*` + 5 `uz_*` symbols (`uz_*` under `--features zenodo`).
- `qfm` — Pauli–Grover + diffusion Hamiltonians, `dense_pauli_grover_matvec`, parity/MNIST sweeps.
- `qfm_text` — text-domain QFM: corpus, features, LM, in-context adaptation, Oxieml decoder, GPU decode sketch.
- `unfer_edge` — Pingora-based edge server for the `unfer_agent` protocol over HTTP (`admin.rs` S22 soft/hard config console under `--features audit`; `gate.rs`/`blueprint.rs`/`cells.rs` edge routes).
- `demo_module/` — first module: `module.toml` + Austral cell + `run_demo.sh` (positive + UK-4001 negative test).
- `bayes_update_module/`, `iterated_bayes_module/`, `qfm_module/`, `qfm_tomo_module/`, `zenodo_store_module/` — 5 more Austral modules.
- `unfer_consensus` — certificate/UTXO ledger (`certs`), unified-auction engine (`auction`), QuePaxa-style consensus engine, signing, multi-node convergence (Plan R).
- `unfer_taler` — GNU Taler exchange adapter over the cert ledger: reserves, two-phase wire gateway, denominations, e-coin withdraw/deposit/peg-out with the `fiat_in - fiat_out = reserves + merchants + outstanding` audit (UK-7101..7107, Plan R Phase 5), plus the operator-side auction settlement service (`auction`, both carbon-credit and publicity markets).
- `unfer_nixvm/` — Nix flake packaging `unfer_ffi` inside the cloud-hypervisor VM guest (see `../../australVM/cloud_hypervisor_vm/`).
- `docs/` — `MODULE_RECIPE.md`, `PROTOCOL.md`, `ARCHITECTURE.md`, `BUILD_PIPELINE.md`.

Sibling repos:
- `australVM/safestos/cranelift` — JIT with `AuthorizationEngine` trait, `uk_*` symbol registration (feature `unfer-kernel`), `modhost` binary.
- `velysterm/crates/kernel_client` — async worker-thread client + `unfer_agent` NDJSON binary + parser.
- `velysterm/crates/mathed_core` — `PropKind::{Model, Prior, Event, Prob}` + `KernelStatement` in `SemanticIndex`; `glyphs` (Bevy-free glyph index); `accessibility` (toolkit-neutral a11y nodes).
- `velysterm/crates/mathed` — Bevy bridge (`kernel_sys.rs`), overlay rendering of prob results.
- `velysterm/crates/mathed_mini` — optional Bevy-free CPU frontend (winit + softbuffer, caret navigation, foot-style layout caching).

## Resolved Limitations

- **CUDA optional** (S1): all tests run CPU-only; `cuda` is additive.
- **Gram robustness** (S2): eigendecomposition whitening replaces bare Cholesky.
- **BRST projection** (S3): proper `project_physical` via CG, not subtraction hack.
- **Explosion bounds** (S4): `SirkOpts` + `compile_expression_bounded`.
- **Navier-Stokes test** (S5): re-enabled, runs the actual solver.
- **Restarted Krylov** (S6): `evolve_restarted` + `reconstruct` for long evolution.
- **Star topology degeneracy**: Single-mode-per-word star topology produces within-class degenerate W-rows because modes sharing the same label have identical Hamiltonian columns. **Distributed multi-mode encoding** (each word is a superposition of 2+ unique dedicated feature modes) breaks this degeneracy and enables 7/7 training accuracy.
- **Gram whitening vs full-rank orthogonalization**: Gram eigendecomposition whitening (with `rel_tol=1e-12` rank truncation) and full-rank orthogonalization (keep all positive eigenvalues) give identical results when no near-null eigenvalues exist. Raw non-orthogonal bases violate unitarity and give random 8/16 classification.
- **Asymmetric label distribution**: Balanced label counts (6e/6o) cause permutation-symmetric Krylov subspace → random 8/16. Asymmetric (5e/7o) breaks symmetry → 12/12 training at m≥3.
- **`compile_channels` API now has `per_mode_weights` parameter**: optional `Option<&HashMap<(u32,u32),f64>>` for per-transition amplitude weights. Pass `None` for uniform λ₁.
- **Krylov dimension m=2 insufficient**: regardless of λ₀ value, m=2 cannot distinguish 12 training inputs in the star topology parity test. Minimum m=3 required.
- **Lambda0 sweet spot**: λ₀=1.0 at m=3 gives 12/12 training; λ₀>1.5 degrades (projector dominates transitions).
- **Anti-learning at m=3**: The 3-dimensional Krylov subspace inverts the label structure at moderate λ₀ (0 < λ₀ ≤ ~5), giving 0% training accuracy. This disappears at λ₀=0 (random) and λ₀≥10 (projector dominates, 100% training even at m=3). The sweet spot m≥5 always works regardless of λ₀.
- **Rank saturation**: For single-mode-per-input parity at any scale (4-bit, 7-bit, 8-bit), the effective gram rank of the Krylov subspace caps at 6. The Krylov dimension m saturates in useful spectral directions at ~6, regardless of mode count (18 to 258) or training set size (12 to 200).
- **No generalization in single-mode star topology**: The Hermitian Hamiltonian with single-mode-per-input encoding achieves 100% training at m≥5 but gives 50% on held-out modes (extrapolation) and only ~54% on within-range held-out (interpolation). Each input is an independent mode with no shared structure — the uniform projector provides no mode-specific generalization.

## Core Dependencies
- `candle-core`: GPU tensor management (with `cuda` feature).
- `mathhook`: High-performance LaTeX and math parsing engine.
- `nalgebra`: High-level linear algebra for the reduced subspace.
- `quantrs2-symengine-pure`: Symbolic expression AST.

---
*Note: This project targets the Millennium Prize requirements for Yang-Mills and Navier-Stokes existence by resolving dynamics over discrete Fock-basis boundaries.*
